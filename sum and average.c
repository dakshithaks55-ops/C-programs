1)#include <stdio.h>

int main() {
    int num1, num2, num3;
    int sum, average;
    printf("Enter any three integers: ");
    scanf("%d %d %d", &num1, &num2, &num3);
    sum = num1 + num2 + num3;
    average = sum / 3
    printf("Sum of the three numbers: %d %d %d\n", sum,average);
     return 0; 
}
